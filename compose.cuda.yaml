services:
  crowbarr:
    build:
      context: .
      dockerfile: Dockerfile.cuda
    image: crowbarr:local-cuda
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
